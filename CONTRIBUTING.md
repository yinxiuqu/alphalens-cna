# 贡献指南

这个库的立身之本是**结论可信**，所以对改动的门槛比一般项目高一点。
下面六条不是形式主义，每条都对应一次真实踩过的坑。

## 提交前必做

```bash
pytest -q                                  # 全套测试
pytest tests/test_compat.py -q -v          # 对拍（需 pip install -e ".[compat]"）
```

CI 会跑三件事，**任何一件红了都不合**：
`test`（3.9/3.11/3.12 矩阵）、`parity`（与 alphalens 逐位对拍）、
`no-plotting-dep`（核心包不许 import 绘图库）。

## 六条硬规矩

### 1. 新逻辑必须带"已知答案测试"
不能只测"跑得通"。测试数据要**能手算**——比如复权因子用 3 只票 2 天的构造，
断言精确到 1e-12。`tests/` 里到处都是这种例子。

### 2. 不许让对拍变红
`compat.check_parity()` 在退化配置下要求三个核心量与 alphalens
**最大差恰好 0.0**（不是"接近"）。这条是"能替换 alphalens"唯一的硬证据。
*如果你觉得必须让某个数变，先想清楚是 alphalens 错了还是你错了 —— 两者都要写进 PR 说明。*

### 3. 不许引入绘图依赖
核心包 `import` 之后不得出现 `matplotlib` / `seaborn` / `plotly`。
要画图请消费 `Report.frames()`（12 张 tidy 表），在自己的脚本里画。

### 4. 不许静默改数字
改动的若影响既有结论，必须在 `CHANGELOG.md` 说明**并给出归因**：
变了多少、为什么变、原来的数字错在哪。本项目的传统是"发现的偏差自己先报"——
`docs/cases/` 里 22 份报告有 3 份是修正自己先前结论的。

### 5. 契约优先
新功能先问：**它需要什么契约对象？** 缺字段就拒绝运行（防线 1），
而不是"先跑起来再说"。私有数据源的适配器放 `examples/`，**不进核心包**。

### 6. 每一步剔除都要能归因
不许 `dropna()` 了事。剔除要进"带原因的账"（`CleanResult.ledger`），
且满足不变量：**输入 = 输出 + 各类剔除之和**，对不上就抛异常。

## 报告 bug

请附上：① 输入数据的结构（列名 + 索引 + 几行样例，**脱敏**）；
② 期望值与实际值；③ 用的是哪个 `PIT_MODE`/`ReturnModel` 配置。
如果涉及"结论不一样"，请说明是否做过体检（`health_check`）与中性化。

## 代码风格

- 中文注释与文档字符串（这个库的读者是中文使用者）
- 注释解释**为什么**，不解释**是什么**；尤其是"这里为什么不能用那个显然的写法"
- 类型标注尽量写全；`from __future__ import annotations`（兼容 3.9）
