# 审稿记录 · IC看不见崩盘：ROE因子全身检查

模式：**内容审**（无 `article.html`，自动识别）
审稿时间：2026-09-22
稿源：`draft.md`（2138 字，7 节 + 附录）

## 第 1 步：环境检查 + 本篇约束

| 项 | 结果 |
|---|---|
| `validate_env.py` | ✅ 退出码 0（用 `--config/--env` 指向 `/home/yinxiuqu/wechat_articles/`；脚本默认按 cwd 找配置，故须显式传参） |
| 账号合并配置 | ✅ `config.yaml` + 本篇 `article.yaml` 已读 |
| `writing-spec.md` | ✅ 存在（用词/引号/加粗/句长 4 条） |
| `presets/review-rules.yaml` | ⚪ 无 → 仅执行内置清单 |
| （缺失项 fallback）`summary_length` / `heading_density` / `forbidden_title_phrases` / `custom_sensitive_words` / `forbidden_words` / `original_attribution` / `closing_block` / `review_output_format` | ⚪ 合并配置中均未配置 → 对应检查项跳过（已在第 3 步逐条标注） |

## 第 2 步：逐项检查

### 标题
| 检查 | 结论 |
|---|---|
| 字数 ≤ `title_max_length`(20) | ✅ 17 字 |
| 无 `forbidden_title_phrases` | ⚪ 未配置，跳过 |
| 与正文一致、非标题党 | ✅ 「IC 看不见崩盘」由第 4 节机制解释坐实；「全身检查」对应 10 项体检 |
| 吸引力 | ✅ 数字型风格；「看不见」制造认知缺口 |

### 摘要
| 检查 | 结论 |
|---|---|
| 长度（`summary_length` 未配置） | ⚪ 跳过；实测 89 字 |
| 概括核心价值 | ✅ 含 3 个毛病 + 首尾 RankIC 对比 + 尾部结论 |
| 与正文一致 | ✅ 数字与底稿一致 |
| 非首段复制 | ✅ 首段是场景叙事，摘要信息密度更高 |

### 正文内容
| 检查 | 结论 |
|---|---|
| 敏感词 / 禁用词 | ⚪ 未配置自定义表 → 人工通读未发现政治/合规风险；无荐股、无收益承诺 |
| 错别字 / 语病 | ✅ 通读无 |
| 事实与数据出处 | ✅ **逐条核对 5 份底稿，全部一致**（见下表） |
| 原创标注 | ⚪ `original_attribution` 未配置 → 跳过 |
| 开头 2–3 句吸引力 | ✅ 「RankIC −0.059 看着挺能用」→「第一步就翻车」 |

**事实核对明细（以底稿为准）**

| 稿中数字 | 底稿出处 | 结论 |
|---|---|---|
| 5,004 只无一只中途消失；5,905 / 339 / **0 只** | 体检层报告 §二 | ✅ |
| 5,066 → 5,316 只 | 体检层报告 | ✅ |
| −0.0589 → −0.0479 → −0.0473 → −0.0400 → −0.0395 | 退市收益约定 §二 | ✅ |
| 实现率 **14%** vs 在市股 **86%** | 退市收益约定 §四 | ✅ |
| Q1 4.4% / Q2 2.0% / Q3 1.7% / Q4 1.7% / Q5 3.2%，t=−3.10 | 尾部统计 §三 | ✅ |
| −0.0272 → +0.0381 → +0.0292 | 中性化后 ROE 复核 §二 | ✅ |
| t：−3.10 / −7.97 / **−8.28** | 中性化后 ROE 复核 §二 | ✅ |
| 训练 t=−4.89（6 个月）/ −0.69（12 个月）；测试 t=−3.32；Q1 2.00% vs Q5 0.28%（7.1 倍） | M1 收尾报告 §一 + `oos_prereg.json` | ✅ |
| Bonferroni 门槛 **2.498**（n_trials=4） | `inference.t_threshold(4)` | ✅ |
| 与 alphalens 三量 **0.000e+00** 逐位相同 | 换手口径与 D9 核实 §五 | ✅ |

### 写作规范合规（对照 `writing-spec.md`）
| 规范 | 结论 |
|---|---|
| 数字用阿拉伯数字 | ✅ |
| 用「」代替引号 | ✅ 弯引号 0 处 |
| 核心观点加粗 | ✅ |
| 单句 ≤40 字、无从句嵌套 | 🟡→✅ **初稿 4 处 41–44 字，已修**（均为顿号枚举/逗号并列，非嵌套从句，属轻微超标） |

### AI 味自检（默认只诊断，🟡 不阻断）
| 指纹 | 结论 |
|---|---|
| 开头万能套话 | ✅ 无「随着…的发展」类 |
| 工整三段式排比扎堆 | ✅ 无 |
| 「不是 X 是 Y」滥用 | ✅ 1 处，未达阈值（800 字内 ≥3 次） |
| 强行拔高升华 / 空洞祝福 | ✅ 无；收尾是具体方法论 3 条 + CTA |
| 概念名词扎堆缺具体 | ✅ 反向：每个概念都跟了具体数字与机制 |
| 收尾 CTA | 🟡 「觉得有用？点个关注…」为本号既有固定收尾（与前 4 篇一致），保留 |

### 配图标记
| 检查 | 结论 |
|---|---|
| 封面标记存在 | ✅ `![封面：…]` |
| 数量与 `image_density`(按需配图) 匹配 | ✅ 1 封面 + 3 信息图 |
| 描述具体 | ✅ 每张都写明了要画什么、用什么数据 |
| 类型与段落匹配 | ✅ 信息图均落在对应论证段之后 |

### 结构
| 检查 | 结论 |
|---|---|
| 小标题密度 | ✅ 7 节 / 1936 字，约 1 节 280 字 |
| 结尾有力 | ✅ 3 条方法论 + 附录项目信息 |
| 篇幅 | ✅ 中文字数 **1936**，落在 `target_word_count` 1800–2500 |
| 文末引导区块 | ⚪ `closing_block` 未配置 → 沿用既有固定收尾 |

### 私有信息泄漏扫描
✅ 无 `/home/`、无私有库名、无数据库名、无内部目录、无个人路径

## 第 3 步：审稿结论

**🔴 必须修改：0 项**
🟡 建议修改：2 项（超长句 4 处 —— **已修**；固定收尾 —— 保留）
🟢 通过：其余全部

**结论：内容审通过，可进入第 5 步定稿。**

## 第 5 步：剥离引用标注 + 文末 embed ⛔

1. **剥离引用标注** ✅ 已按 skill 要求**用脚本执行**（禁止手工撕）：
   ```
   write.py strip-citations draft.md -o draft-stripped.md
   [OK] 已剥离 0 处引用标注
   ```
   本篇无引用标注 → 剥离后内容与 `draft.md` 一致。

2. **文末 embed —— 无配置，已跳过（显式标注，非静默跳过）**

   | 占位符 | 合并后配置 | 处理 |
   |---|---|---|
   | `{embed:profile:…}` | `embeds.profiles[0].name = ''` → **无效项** | 不追加 |
   | `{embed:miniprogram:…}` / `{embed:miniprogram_card:…}` | `embeds.miniprograms[0].title = ''` → **无效项** | 不追加 |
   | `{embed:link:…}` | `embeds.related_articles.manual = []`（空） | 不追加 |

   ⚠️ **说明**：`related_articles.strategy = auto`、`auto_count = 3`。
   按 review skill 规则，`manual` 为空时本环节**不自动补齐**（保持纯本地、零网络）；
   可由 **publish 环节**在发布前调用 `getdraft.py published-fields` 自动补 3 条往期推荐，
   或由用户在 `article.yaml` 手填。**本篇按合法省略处理，`article.md` 文末无 embed 占位符。**

## 定稿

`article.md` = `draft-stripped.md`（已剥离引用标注；按上表合法省略文末 embed）。

---

# 终审（排版 + 配图完成后）

**模式：终审**（`article.html` 已存在）

| 检查项 | 结论 |
|---|---|
| `article.html` 存在且完整 | ✅ 33.5 KB，由**当前** `article.md` 重新生成（配图替换后又跑了一次 `format.py`） |
| HTML 无乱码/格式异常 | ✅ 抽查标题、导语、表格、图片标签均正常 |
| `imgs/` 目录存在 | ✅ 3 张信息图 + 1 张封面 |
| 封面已生成 | ✅ `cover.jpg`（1792×763，2.35:1） |
| **所有 placeholder 已替换** | ✅ `article.md` 0 处 / `article.html` 0 处 |
| 图片文件实际存在 | ✅ `src="imgs/*.png"` 三个路径与磁盘文件一一对应 |
| 标题/摘要/作者就绪 | ✅ `article.yaml`：title 17 字、digest 90 字、author CowBoy |
| 正文无草稿标记 | ✅ |
| 评论设置 | ✅ `need_open_comment: 1`、`only_fans_can_comment: 0`（已在全局 config） |

## 终审结论

**✅ 通过。产物可直接用于 `publish.py full`。**

### 交代三件事（留给主 agent 决策）

1. **封面是 matplotlib 生成的图表式封面**，不是 AI 插画 —— 因 `IMAGE_MODEL_API_KEY` 未配置，
   走了 images skill 允许的 Agent 降级路径。若你更想要插画风封面，配置图片模型后
   用 `make_figs.py` 之外的方式重出即可，正文三张信息图不受影响。
2. **文末 embed 为空配置，已合法省略**（详见上文第 5 步表格）。
   `related_articles.strategy: auto`，可由 publish 环节自动补 3 条往期推荐；
   你们已有 4 篇已发文可作候选。
3. **`article.yaml` 的封面字段名已从 `cover` 订正为 `cover_image`** ——
   与 schema（`articlescreening-schema.md` 第 42 行）和上一篇已发文的写法一致。
