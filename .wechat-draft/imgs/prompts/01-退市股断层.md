---
aspect: "16:9"
---
信息图：三段横向漏斗条，逐级变窄 —— 「数据库里有行情的股票 5,905 只」→「其中已标记退市 339 只」
→「进入分析缓存的退市股 0 只」。最后一段用**红色虚线空框**表示"什么都没有"，
右侧一支红色向下箭头标注「断在这里」。底部一行小字说明根因：建缓存的脚本只从「在市公司」表取代码。
（本次由 `make_figs.py` 生成，数值出处见 `img_analysis.md`。）
