# 配图记录

**图片来源：`image_source: generated`** —— 但外部图片模型**未配置**
（`aws.env` 的 `IMAGE_MODEL_API_KEY` 为空，`image_create.py` 会以退出码 2 / `[NO_MODEL]` 退出），
故按 images skill 的**降级路径**处理：**由 Agent 用 matplotlib 生成图表式配图**，
配色对齐排版预设「技术 / 蓝」（主色 `#2563EB`，强调 `#DC2626`）。

> ⚠️ 与 AI 生图的差别：这一类是**数据可视化**而非插画，所有数值均可回溯到 `outputs/` 底稿，
> 不存在"模型画错数字"的风险；生成脚本 `make_figs.py` 一并留在本篇目录，可复跑。

| 文件 | 用途 | 画面 | 数据出处 |
|---|---|---|---|
| `cover.jpg` | **封面（仅此 1 张）** | 深蓝底；左侧标题「IC看不见崩盘 / ROE 因子全身检查」；右侧一条走势线在末端断裂，放大镜停在断裂处 | 文章标题与主线；走势为示意，不含具体数值 |
| `imgs/01-退市股断层.png` | 正文 | 漏斗式三段：5,905 只有行情 → 339 只已标记退市 → **0 只**进入分析缓存（红色虚框） | `体检层报告.md` §二 |
| `imgs/02-腰斩率U型.png` | 正文 | Q1–Q5 腰斩率柱状：4.4% / 2.0% / 1.7% / 1.7% / 3.2%，标注"两端都不安全，中间最稳"；页脚给 t=−3.10、门槛 −50% | `尾部统计与IC盲区.md` §三（h=252） |
| `imgs/03-RankIC轨迹.png` | 正文 | 五节点折线：−0.0589 → −0.0479 → −0.0473 → −0.0400 → **+0.0292**，跨 0 轴，标注"符号翻转" | `退市收益约定…md` §二 + `中性化后ROE复核.md` §二 |

**硬性约束自检**
- 封面用途「封面」在本文档中出现 **1 次** ✓
- `article.md` / `article.html` 中 `placeholder` 残留 **0** 处 ✓
- 封面未直接复制素材库文件（本机无 `.aws-article/products/`），系本篇原创生成 ✓
